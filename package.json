{
  "scripts": {
    "clean": "powershell.exe remove-item allure-results/*",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.14.1",
    "experimental-allure-playwright": "^0.0.3"
  }
}
